volumes:
  vgm_source_database_local_postgres_data: {}
  vgm_source_database_local_postgres_data_backups: {}
  

services:
  django:
    build:
      context: .
      dockerfile: ./compose/local/django/Dockerfile
    image: vgm_source_database_local_django
    container_name: vgm_source_database_local_django
    depends_on:
      - postgres
    volumes:
      - /app/.venv
      - .:/app:z
    env_file:
      - ./.envs/.local/.django
      - ./.envs/.local/.postgres
    ports:
      - '8000:8000'
    command: /start

  postgres:
    build:
      context: .
      dockerfile: ./compose/production/postgres/Dockerfile
    image: vgm_source_database_production_postgres
    container_name: vgm_source_database_local_postgres
    volumes:
      - vgm_source_database_local_postgres_data:/var/lib/postgresql/18/docker
      - vgm_source_database_local_postgres_data_backups:/backups
    env_file:
      - ./.envs/.local/.postgres
