"""App configuration for config app."""

from django.apps import AppConfig


class ConfigConfig(AppConfig):
    """Configuration for the config app."""

    name = "config"
    verbose_name = "Configuration"
